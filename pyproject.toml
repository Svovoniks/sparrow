[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Sparrow"
requires-python = ">= 3.8"
versin = 0.1

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

